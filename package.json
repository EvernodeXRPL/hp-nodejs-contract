{
    "name": "hotpocket-nodejs-contract",
    "version": "0.5.0",
    "scripts": {
        "build": "mkdir -p dist && cp src/hp-contract-lib.js dist/index.js && ./clean-pkg.sh",
        "publish": "npm run build && npm publish dist"
    }
}
