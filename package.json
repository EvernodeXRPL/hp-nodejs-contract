{
    "name": "hotpocket-nodejs-contract",
    "version": "0.5.5",
    "scripts": {
        "build": "ncc build src/index.js --target es2021 -o dist && ./clean-pkg.sh",
        "publish": "npm run build && npm publish ./dist"
    }
}
